---
title: AI Notebook 1 — Основа Python
sidebar_position: 6
description: Типы данных, ветвления, циклы и первые вычисления с NumPy.
slug: /python/ai-notebook-01-python-basics
---

## Теория

Первая рабочая тетрадь закрепляет базовый синтаксис Python и переход к численным вычислениям.

## Код из решения

```python title="Notebook1.ipynb (типы и структуры)"
x = 3 + 5.2 * 7
y = None
z = 'a', 5, 12.345, (2, 'b')
df = [['Антонова Антонина', 34, 'ж'], ['Борисов Борис', 26, 'м']]
A = {1, 'title', 2, 'content'}

print(x, '|', type(x))
print(y, '|', type(y))
print(df, '|', type(df))
print(A, '|', type(A))
```

```python title="Notebook1.ipynb (статистика случайного массива)"
import numpy as np
import matplotlib.pyplot as plt

size = 10
x = range(0, size)
random = np.random.random_sample(size=size)
average = np.average(random)
median = np.median(random)

fig, ax = plt.subplots(figsize=(10, 6))
ax.scatter(x, random)
plt.show()
```

## Разбор

Материал даёт базу для следующих тетрадей: переменные, условия, циклы, срезы и первый анализ массивов.

Исходник: `pr_Python/Pr_Ai/Notebook1.ipynb`.

## Вывод

Notebook 1 подготавливает к работе с данными и библиотеками научного Python-стека.
