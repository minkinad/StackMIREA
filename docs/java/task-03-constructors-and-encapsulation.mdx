---
title: Task 3 — Конструкторы и инкапсуляция
sidebar_position: 4
description: Инициализация объектов и контроль состояния через инкапсуляцию.
slug: /java/task-03-constructors-and-encapsulation
---

## Теория

Инициализация объектов и контроль состояния через инкапсуляцию. Практика собрана из реальных решений в каталоге `pr_Java/Task3`.

## Код из решения

```java title="TestAuthor.java"
package Task3;

public class TestAuthor {
    public static void main(String[] args){
        Author a1 = new Author("Petr Grob", "peGrob@somewhere.com", 'M');
        System.out.println(a1);
    }

}
```

## Что в исходниках

- `pr_Java/Task3/Author.java`
- `pr_Java/Task3/Ball.java`
- `pr_Java/Task3/TestAuthor.java`
- `pr_Java/Task3/TestBall.java`

## Разбор

В этом модуле используется 4 Java-файлов. Ключевые сущности: Author, Ball, TestAuthor, TestBall.

:::tip
Для проверки практики сначала запускайте тестовый/демо-класс из папки задачи, затем расширяйте модель новыми кейсами.
:::

## Вывод

Task 3 — Конструкторы и инкапсуляция теперь отражает фактическую реализацию из `pr_Java/Task3`, а не шаблонный пример.
