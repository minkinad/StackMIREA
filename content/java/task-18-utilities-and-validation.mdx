---
title: Task 18 — Утилиты и валидация
sidebar_position: 19
description: Практика с матрицами, калькулятором и проверкой входных данных.
slug: /java/task-18-utilities-and-validation
---

## Теория

Практика с матрицами, калькулятором и проверкой входных данных. Практика собрана из реальных решений в каталоге `pr_Java/Task18`.

## Код из решения

```java title="Calculator.java"
package Task18;

public class Calculator<T extends Number, V extends Number> {
    private T a;
    private V b;

    public Calculator(T a, V b) {
        this.a=a;
        this.b=b;
    }

    public double sum(){
        return a.doubleValue()+b.doubleValue();
    }
    public double multiply(){
        return a.doubleValue()*b.doubleValue();
    }
    public double divide(){
        return a.doubleValue()-b.doubleValue();
    }
    public double subtraction(){

            return a.doubleValue() / b.doubleValue();

    }
    public static void main(String[] args){
        Calculator<Integer, Integer> r = new Calculator<>(1, 0);
        System.out.println("Сумма: "+r.sum());
        System.out.println("Вычитание: " + r.divide());
        System.out.println("Умножение: " +r.multiply());
        System.out.println("Деление: " + r.subtraction());
    }
}
```

## Что в исходниках

- `pr_Java/Task18/Animal.java`
- `pr_Java/Task18/Calculator.java`
- `pr_Java/Task18/Exeption1.java`
- `pr_Java/Task18/Matrix.java`
- `pr_Java/Task18/MinMax.java`

## Разбор

В этом модуле используется 5 Java-файлов. Ключевые сущности: Animal, Calculator, Exeption1, Matrix, MinMax.

:::tip
Для проверки практики сначала запускайте тестовый/демо-класс из папки задачи, затем расширяйте модель новыми кейсами.
:::

## Вывод

Task 18 — Утилиты и валидация теперь отражает фактическую реализацию из `pr_Java/Task18`, а не шаблонный пример.
