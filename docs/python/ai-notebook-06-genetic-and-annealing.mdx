---
title: AI Notebook 6 — Генетика и отжиг
sidebar_position: 11
description: Эволюционные шаги и эвристический поиск маршрутов.
slug: /python/ai-notebook-06-genetic-and-annealing
---

## Теория

Тетрадь содержит две эвристики оптимизации: генетические шаги и имитацию отжига.

## Код из решения

```python title="Notebook6.ipynb (генетический шаг)"
def qZ(x, y):
    return (x - 3 * y + 1) / (3 * x ** 2 + 3 * y ** 2 + 1)

def evoStep(x, y, z):
    _, minId = min((value, idx) for idx, value in enumerate(z))
    x = x[:]
    y = y[:]
    z = z[:]
    x.pop(minId)
    y.pop(minId)
    z.pop(minId)
    return x, y, z
```

```python title="Notebook6.ipynb (длина маршрута)"
def edgeLength(i, j, distances, roundTrip=True):
    if roundTrip:
        return max([(item[2] if (item[0] == i and item[1] == j) or (item[1] == i and item[0] == j) else -1) for item in distances])
    return max([(item[2] if (item[0] == i and item[1] == j) else -1) for item in distances])


def routeLength(V, distances):
    edges = []
    for i in range(len(V) - 1):
        edges.append(edgeLength(V[i], V[i + 1], distances))
    return sum(edges)
```

## Разбор

Показаны эвристические методы без гарантии глобального оптимума, но с хорошей практической применимостью.

Исходник: `pr_Python/Pr_Ai/Notebook6.ipynb`.

## Вывод

Notebook 6 демонстрирует подходы к сложным задачам оптимизации, где полный перебор слишком дорогой.
