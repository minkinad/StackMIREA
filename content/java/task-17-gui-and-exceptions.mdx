---
title: Task 17 — GUI + исключения
sidebar_position: 18
description: Связка UI-слоя, доменной логики и пользовательских ошибок.
slug: /java/task-17-gui-and-exceptions
---

## Теория

Связка UI-слоя, доменной логики и пользовательских ошибок. Практика собрана из реальных решений в каталоге `pr_Java/Task17`.

## Код из решения

```java title="FIOINN.java"
package Task17;

import java.util.Scanner;

public class FIOINN {
    public void red() {
        System.out.println("Введите ФИО");
        Scanner fi = new Scanner(System.in);
        String fio = fi.nextLine();
        System.out.println("Введите ИНН");
        Scanner in = new Scanner(System.in);
        String[] inn = in.nextLine().split("");
        try {
            if (inn.length == 12) {
                System.out.println("ИНН " + fio + " верный!");
            } else {
                throw new Proverka(fio);
            }
        }catch (Proverka e){
            System.out.println(e.getMessage());
        }
    }
    public static void main(String[] args){
        FIOINN n = new FIOINN();
        n.red();
    }
}
```

## Что в исходниках

- `pr_Java/Task17/EmptyStringException.java`
- `pr_Java/Task17/FIOINN.java`
- `pr_Java/Task17/LabClass.java`
- `pr_Java/Task17/LabClassDriver.java`
- `pr_Java/Task17/LabClassUI.java`
- `pr_Java/Task17/Proverka.java`
- `pr_Java/Task17/Student.java`
- `pr_Java/Task17/StudentNotFoundException.java`

## Разбор

В этом модуле используется 8 Java-файлов. Ключевые сущности: EmptyStringException, FIOINN, LabClass, LabClassDriver, LabClassUI, Proverka.

:::tip
Для проверки практики сначала запускайте тестовый/демо-класс из папки задачи, затем расширяйте модель новыми кейсами.
:::

## Вывод

Task 17 — GUI + исключения теперь отражает фактическую реализацию из `pr_Java/Task17`, а не шаблонный пример.
