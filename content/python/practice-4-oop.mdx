---
title: Практика 4 — OOP
sidebar_position: 2
description: Классы, AST-структуры и Visitor-подход на примерах из ноутбука.
slug: /python/practice-4-oop
---

## Теория

Практика построена вокруг трёх уровней ООП:

- простые классы с состоянием и методами;
- композиция объектов в дереве выражений;
- паттерн `Visitor` для разделения операций и структуры AST.

## Код из решения

```python title="pract4.ipynb (AST + Visitor)"
class Num:
    def __init__(self, value):
        self.value = value

    def accept(self, visitor):
        return visitor.visit_num(self)

class Add:
    def __init__(self, left, right):
        self.left = left
        self.right = right

    def accept(self, visitor):
        return visitor.visit_add(self)

class Mul:
    def __init__(self, left, right):
        self.left = left
        self.right = right

    def accept(self, visitor):
        return visitor.visit_mul(self)

class PrintVisitor:
    def visit_num(self, node):
        return str(node.value)

    def visit_add(self, node):
        return f"({node.left.accept(self)} + {node.right.accept(self)})"

    def visit_mul(self, node):
        return f"({node.left.accept(self)} * {node.right.accept(self)})"

class CalcVisitor:
    def visit_num(self, node):
        return node.value

    def visit_add(self, node):
        return node.left.accept(self) + node.right.accept(self)

    def visit_mul(self, node):
        return node.left.accept(self) * node.right.accept(self)
```

## Разбор

`Num`, `Add`, `Mul` хранят только структуру выражения, а `PrintVisitor` и `CalcVisitor` реализуют разные операции над одним и тем же деревом. Это удобный шаблон, когда операции растут быстрее, чем количество типов узлов.

Исходник практики: `pr_Python/pr_Python/pract4.ipynb`.

## Важные моменты

:::tip
Если появляются несколько независимых операций (печать, вычисление, компиляция), выносите их в отдельных visitor-классы.
:::

:::warning
Часть кода в ноутбуке учебная и не всегда production-ready; перед переносом в проект добавляйте типизацию и тесты.
:::

## Вывод

Практика закрепляет переход от простых классов к объектной модели выражений с расширяемыми операциями.
